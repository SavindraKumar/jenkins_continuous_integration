pipeline {
   agent any

   stages {
      stage('static_analysis') {
         steps {
            echo 'Static analysis'
         }
      }
      
   stages {
      stage('build') {
         steps {
            echo 'Build'
         }
      }      
      
   }
}
